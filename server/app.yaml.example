runtime: nodejs18
instance_class: F1

env_variables:
  NODE_ENV: "production"
  # Add your environment variables here
  # DATABASE_URL: "your-database-url"
  # API_KEY: "your-api-key"

handlers:
  - url: /.*
    script: auto
    secure: always 